meta {
  name: Get imported users
  type: http
  seq: 2
}

get {
  url: {{baseUrl}}/api/v1/user-imports
  body: none
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

vars:post-response {
  firstImportId: res.body.data.at(0).id
}

assert {
  res.body.data.at(0).id: isDefined 
}
