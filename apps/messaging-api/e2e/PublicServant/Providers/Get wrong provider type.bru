meta {
  name: Get wrong provider type
  type: http
  seq: 6
}

get {
  url: {{baseUrl}}/api/v1/providers/{{smsProviderId}}?type=email
  body: none
  auth: bearer
}

query {
  type: email
}

auth:bearer {
  token: {{accessToken}}
}

assert {
  res.status: eq 404
  res.body.code: isDefined
  res.body.detail: isDefined
  res.body.requestId: isDefined
  res.body.name: isDefined
}
