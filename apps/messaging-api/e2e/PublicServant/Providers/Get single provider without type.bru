meta {
  name: Get single provider without type
  type: http
  seq: 7
}

get {
  url: {{baseUrl}}/api/v1/providers/{{smsProviderId}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

assert {
  res.status: eq 422
  res.body.code: isDefined
  res.body.detail: isDefined
  res.body.requestId: isDefined
  res.body.name: isDefined
}
