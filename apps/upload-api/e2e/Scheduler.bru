meta {
  name: Scheduler
  type: http
  seq: 3
}

post {
  url: {{baseUrl}}/api/v1/jobs
  body: formUrlEncoded
  auth: none
}

body:form-urlencoded {
  schedulerToken: schedulerToken
}

assert {
  res.status: eq 200
  res.body.status: eq 'ok'
}
