meta {
  name: Delete file sharing
  type: http
  seq: 2
}

delete {
  url: {{baseUrl}}/api/v1/permissions
  body: formUrlEncoded
  auth: none
}

headers {
  Authorization: Bearer {{accessToken}}
}

body:form-urlencoded {
  fileId: {{fileId}}
  userId: user-id
}

assert {
  res.status: eq 200
}
