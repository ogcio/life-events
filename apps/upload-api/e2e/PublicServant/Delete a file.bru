meta {
  name: Delete a file
  type: http
  seq: 2
}

delete {
  url: {{baseUrl}}/api/v1/files/:fileId
  body: none
  auth: none
}

params:path {
  fileId: {{fileId}}
}

headers {
  Authorization: Bearer {{accessToken}}
}

assert {
  res.status: eq 200
}
